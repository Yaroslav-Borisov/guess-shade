export enum Paths {
    Start = '/',
    Game = '/game',
    Results = '/results'
}

export enum Opacity {
    Ordinary = 'Ordinary',
    Special = 'Special',
}

export const GUESSES = 3;

export const GAME_TIME = 60;